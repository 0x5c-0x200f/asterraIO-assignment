import React  from 'react';
import TableResults from './TableResults';

const AppContainer = () => {
  return (
      <div>
        <TableResults />
      </div>
  );
};

export default AppContainer;
